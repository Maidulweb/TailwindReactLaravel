import React from 'react';

const AccessError = () => {
    return (
      <div className="flex items-center h-screen">
        <h2 className="text-center w-full text-red-500">
          You have no access!!!!!
        </h2>
      </div>
    );
};

export default AccessError;